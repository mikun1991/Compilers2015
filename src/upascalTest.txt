program helloWorld;
	var cat, kitten:String;
	myBool:Boolean;
	counter:Integer;
  
	function monkey(var dog:Boolean):Boolean;
		var mBool:Boolean;
		begin
			dog := true;
			if dog 
			then 
				while dog 
				do
					begin
						cat := true;
						dog := false
					end
		end
	;
	begin
	
		for counter:=0 to 9 do write(counter);
	
		while counter > 0 do
			begin
				writeln(counter);
				counter := counter - 1
			end
	
	end
.